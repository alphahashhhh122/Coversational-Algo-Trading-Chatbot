apiVersion: v1
kind: Service
metadata:
  name: iimc-platform
  labels:
    app.kubernetes.io/name: iimc-platform
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: iimc-platform
  ports:
    - name: http
      port: 80
      targetPort: http
